# ---- Devin API (v1) ----
# Personal API key (apk_user_*) or service key (apk_*)
DEVIN_API_KEY=apk_user_xxxxxxxxxxxxxxxx
DEVIN_API_BASE=https://api.devin.ai/v1
# Cap spend per session so a runaway fix can't burn your budget.
DEVIN_MAX_ACU_LIMIT=10
# Set to 1 to run the whole pipeline end-to-end with a simulated Devin
# (no API key, no spend). Great for the recorded demo and CI.
DEVIN_MOCK=1
# In mock mode, how long a session "works" before opening a PR (seconds).
DEVIN_MOCK_WORK_SECONDS=20

# ---- GitHub ----
# Your FORK, as owner/repo (e.g. catherine-zhao/superset)
GITHUB_REPO=your-org/superset
# Token with repo scope (used to comment PR links back onto issues + create issues)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxx
GITHUB_API_BASE=https://api.github.com
# Shared secret configured on the GitHub webhook (Settings > Webhooks)
GITHUB_WEBHOOK_SECRET=replace-with-a-long-random-string
# For live webhook delivery via `docker compose --profile webhook up` (ngrok).
NGROK_AUTHTOKEN=

# ---- Pipeline behavior ----
# Only issues carrying this label are remediated.
TRIGGER_LABEL=devin-fix
# How often the poller reconciles open sessions (seconds).
POLL_INTERVAL_SECONDS=10
# SQLite location (mounted to a volume in docker-compose).
DB_PATH=/data/autofix.db
# Set to 0 to disable the in-process background poller.
RUN_POLLER=1

# ---- Cost governance ----
# Max Devin sessions running at once; excess dispatches queue and are promoted
# as capacity frees (so a big scan can't fan out into many full-suite runs).
MAX_ACTIVE_SESSIONS=3
# Fresh randomized seed orderings the validator re-runs (on top of known-bad
# seeds). Higher = more statistical confidence, more wall-clock/ACU.
VALIDATOR_FRESH_SEED_RUNS=5

# ---- Devin platform primitives (optional) ----
# Machine Snapshot id of a pre-built Superset dev env (kills per-session setup ACU).
DEVIN_SNAPSHOT_ID=
# Comma-separated Knowledge base ids injected into every session.
DEVIN_KNOWLEDGE_IDS=
# Playbook id: a default, and/or a per-flake-class override (DEVIN_PLAYBOOK_<CLASS>).
DEVIN_PLAYBOOK_DEFAULT=
DEVIN_PLAYBOOK_STATE_ISOLATION=
